import React from "react"
import logo from "../../styles/img/logo/logo.png"
export default function Logo(props){
    return(
        <div id="logoContainer">
            <img src={logo} alt="fishing"/>
        </div>
    )
}